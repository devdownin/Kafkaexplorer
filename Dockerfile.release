# Dockerfile optimisé pour le déploiement de JAR pré-buildés
FROM eclipse-temurin:21-jre
WORKDIR /app

# On s'attend à ce que le JAR soit nommé app.jar (téléchargé par update.ps1)
COPY app.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
