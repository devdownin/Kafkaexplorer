<?xml version="1.0" ?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/>
	</parent>
	<groupId>com.compagnonsdudev</groupId>
	<artifactId>kafka-sql-explorer</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>kafka-sql-explorer</name>
	<description>Kafka SQL Explorer with Flink Embedded</description>
	<licenses>
		<license>
			<name>GNU Affero General Public License, Version 3.0</name>
			<url>https://www.gnu.org/licenses/agpl-3.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>
	<properties>
		<java.version>25</java.version>
		<flink.version>2.3.0</flink.version>
		<kafka.version>4.3.1</kafka.version>
		<confluent.version>8.3.1</confluent.version>
		<!--
			Surefire's own default, declared explicitly so `${skipTests}` always resolves:
			the frontend test execution reads it to mirror -DskipTests, and an undefined
			property there would reach the plugin as the literal string.
		-->
		<skipTests>false</skipTests>
		<!--
			The JUnit tag filter surefire reads. Declared here so both halves always resolve to
			something (an undefined property reaches the plugin as its literal `${...}` text), and
			so the `llm-eval` profile has one place to flip them.
		-->
		<test.excluded.groups>llm-eval</test.excluded.groups>
		<test.groups />
	</properties>
	<repositories>
		<repository>
			<id>confluent</id>
			<url>https://packages.confluent.io/maven/</url>
		</repository>
	</repositories>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-streaming-java</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-api-java-bridge</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-api-java</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-planner_2.12</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-runtime</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-clients</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-runtime-web</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-connector-base</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-connector-kafka</artifactId>
			<version>5.0.0-2.2</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-json</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-avro</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-avro-confluent-registry</artifactId>
			<version>${flink.version}</version>
		</dependency>
		<dependency>
			<groupId>com.jayway.jsonpath</groupId>
			<artifactId>json-path</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.kafka</groupId>
			<artifactId>kafka-clients</artifactId>
		</dependency>
		<dependency>
			<groupId>io.confluent</groupId>
			<artifactId>kafka-avro-serializer</artifactId>
			<version>${confluent.version}</version>
		</dependency>
		<dependency>
			<groupId>io.confluent</groupId>
			<artifactId>kafka-schema-registry-client</artifactId>
			<version>${confluent.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.avro</groupId>
			<artifactId>avro</artifactId>
			<version>1.12.2</version>
		</dependency>
		<dependency>
			<groupId>jakarta.xml.bind</groupId>
			<artifactId>jakarta.xml.bind-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.glassfish.jaxb</groupId>
			<artifactId>jaxb-runtime</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>com.anthropic</groupId>
			<artifactId>anthropic-java</artifactId>
			<version>2.54.0</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-kafka</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!--
					  The Process Mining eval has two halves. The deterministic one (the aggregate
					  over the seeded demo pipeline) is an ordinary unit test and runs here. The
					  loose one calls a real model: it costs money, needs the network, and its
					  verdict depends on which model is configured — none of which belongs in a
					  build that has to be reproducible and free. It is tagged and excluded, and
					  the `llm-eval` profile below is how it is run deliberately.

					  Both directions were measured against surefire 3.5.6 rather than assumed,
					  because the failure mode of getting it wrong is a build that runs *no* test
					  and stays green: an empty `groups` is ignored (default = every untagged
					  test), and the profile's empty `excludedGroups` plus `groups=llm-eval` runs
					  the tagged one alone.
					-->
					<excludedGroups>${test.excluded.groups}</excludedGroups>
					<groups>${test.groups}</groups>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>3.6.3</version>
				<executions>
					<execution>
						<id>enforce-versions</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireMavenVersion>
									<version>3.9.0</version>
								</requireMavenVersion>
								<requireJavaVersion>
									<version>25</version>
								</requireJavaVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<url>https://github.com/devdownin/Kafkaexplorer</url>
	<developers>
		<developer>
			<id>owner</id>
			<name>Compagnons du dev</name>
			<email>contact@compagnonsdudev.com</email>
			<organization>Compagnons du dev</organization>
			<organizationUrl>https://compagnonsdudev.com</organizationUrl>
		</developer>
	</developers>
	<scm>
		<connection>scm:git:git://github.com/devdownin/Kafkaexplorer.git</connection>
		<developerConnection>scm:git:ssh://git@github.com/devdownin/Kafkaexplorer.git</developerConnection>
		<url>https://github.com/devdownin/Kafkaexplorer/tree/main</url>
	</scm>
	<profiles>
		<!--
		  Runs the LLM half of the Process Mining eval, and only it: `-P llm-eval` flips the tag
		  filter from "exclude" to "select", so the build does not also replay 890 unit tests to
		  reach three that call an endpoint. The test skips itself, rather than failing, when no
		  provider and key are configured — see LlmAnalysisEvalTest.
		-->
		<profile>
			<id>llm-eval</id>
			<properties>
				<test.excluded.groups />
				<test.groups>llm-eval</test.groups>
			</properties>
		</profile>
		<profile>
			<id>build-frontend</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>com.github.eirslett</groupId>
						<artifactId>frontend-maven-plugin</artifactId>
						<version>2.0.2</version>
						<configuration>
							<workingDirectory>src/main/webapp</workingDirectory>
							<installDirectory>target</installDirectory>
						</configuration>
						<executions>
							<execution>
								<id>install node and npm</id>
								<goals>
									<goal>install-node-and-npm</goal>
								</goals>
								<configuration>
									<nodeVersion>v24.0.0</nodeVersion>
								</configuration>
							</execution>
							<execution>
								<id>npm install</id>
								<goals>
									<goal>npm</goal>
								</goals>
								<configuration>
									<arguments>install</arguments>
								</configuration>
							</execution>
							<execution>
								<id>npm run build</id>
								<goals>
									<goal>npm</goal>
								</goals>
								<configuration>
									<arguments>run build</arguments>
								</configuration>
							</execution>
							<!--
								The frontend checks were previously reachable only by hand: `mvn verify`
								ran `npm run build` (tsc + vite) and nothing else, so ESLint and the
								Vitest suite never ran in CI and a broken pure module (queryError,
								sqlScope, windowSql, resultExport) went green.

								Bound to `verify` rather than `test` on purpose: it keeps
								`mvn test -Dtest=SomeJavaTest` a Java-only, fast loop, and `mvn package
								-DskipTests` never reaches this phase — so the release workflow, which
								packages with -DskipTests, is unaffected. `mvn verify` is the complete
								gate, and it is what CI runs.
							-->
							<execution>
								<id>npm run lint</id>
								<phase>verify</phase>
								<goals>
									<goal>npm</goal>
								</goals>
								<configuration>
									<arguments>run lint</arguments>
								</configuration>
							</execution>
							<execution>
								<id>npm test</id>
								<phase>verify</phase>
								<goals>
									<goal>npm</goal>
								</goals>
								<configuration>
									<arguments>test</arguments>
									<!-- Honours -DskipTests like the Java suite does. -->
									<skip>${skipTests}</skip>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>