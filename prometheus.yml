global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'kafka-sql-explorer'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['explorer:8080']
