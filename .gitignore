# Maven build artifacts
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# IDEs
.idea/
*.iml
*.iws
.project
.classpath
.settings/
.factorypath
.vscode/

# Local configuration
application-local.yml
application-local.properties

# Logs and PID files
logs/
# The Flink job store (explorer.flink-job-store-path). Written at runtime and mounted as a
# volume in every stack — CLAUDE.md already described it as gitignored, which it was not:
# running the suite produced data/flink-jobs.json and it turned up in a commit.
data/
*.log
*.pid
app.pid
app.log

# Temporary and prompt files
part*.txt
/home/jules/verification/

# OS files
.DS_Store
Thumbs.db

# Frontend build artifacts
src/main/resources/static/assets/
src/main/resources/static/index.html

# Node.js
node_modules/
dist/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Runtime logs (logging.file.name: logs/kafkaexplorer.log)
logs/

# Bytecode Python des vérificateurs de docs/ — généré à chaque exécution locale.
__pycache__/
*.py[cod]

# TypeScript incremental build info (tsc -b)
src/main/webapp/tsconfig.tsbuildinfo

# Node toolchain fetched by a local build (frontend-maven-plugin / npx).
.node/
